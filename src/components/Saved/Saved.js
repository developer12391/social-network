import React from 'react';
import s from'./Saved.module.css';

const Saved = (props) => {
    return (
        <div className={s.content}>
            Saved
        </div>
    );
}


export default Saved;